package exceptions;

public class MoreExceptions_TempException extends Exception{
	
	/**
	 * This can be used as the main exception for the toocold and toohot exceptions.
	 * To do this, this class must be an extension of the exception class with
	 * the tooCold and tooHot class as extensions of the TempException class.
	 * 
	 * 		ie:
	 * 			public class tooCold extends tempException (not Exception).
	 * 
	 */
	
}
